const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('file:///' + __dirname.replace(/\\/g, '/') + '/index.html'); const result = await page.evaluate(() => { openProjectModal('miami-beach-condo'); return document.getElementById('project-modal-overlay').innerHTML; }); const count = (result.match(/<img src=/g) || []).length; console.log('Images:', count); await browser.close(); })();
